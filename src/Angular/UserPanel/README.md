# Ntk.Asterisk UserPanel (Angular 19)

پنل کاربر برای صدور و پیگیری CallJob — بدون تنظیمات AMI و بدون احراز هویت.

| مورد | مقدار |
|------|--------|
| Dev port | `5312` |
| API | `http://localhost:5310` (`environment*.ts`) |
| Hub | `/hubs/asterisk` |

```powershell
cd src/Angular/UserPanel
npm start
# or
npm run build
```

Routes: `/calls` · `/jobs`
